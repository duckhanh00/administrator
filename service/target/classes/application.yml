spring:
  cache:
    type: redis
  redis:
    host: 127.0.0.1
    port: 6379
    password: password
    database: 0
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0